# 2026-07-13 Daily Notes

## 02:13 ~ 02:30 — 全量重构（第二轮）

### 已完成文件
- **Windows 端（5 文件）**：
  - `Program.cs`：添加 `using System.Threading.Tasks`，`LogCrash` 改为 `internal`，全局异常捕获
  - `App.axaml.cs`：Initialize/OnFrameworkInitializationCompleted 添加 try-catch，记录初始化异常
  - `MainWindow.axaml`：移除 TrayIcon（XAML 结构冲突），保持简洁布局
  - `MainWindow.axaml.cs`：提取 `StartAudioEngine`/`StartHandshakeServer` 方法，添加 `UpdateStateIndicator`，添加系统托盘事件处理（OnTrayShowWindow/OnTrayExit），添加异常处理
  - `AudioEngine.cs`：提取 `DecodeAndProcess`、`CheckWatchdog`、`ResetWatchdog` 方法，添加 Start/Stop/RecvLoop 异常处理，改进日志记录
- **Android 端（2 文件）**：
  - `MainActivity.kt`：提取 `buildPacketHeader`、`parseRouteMode`、`sendUdpPacket`、`recvUdpResponse` 方法，添加注释
  - `ReconnectionManager.kt`：已简洁无需大改

### 编译验证
- Windows 端：`dotnet build` 0 错误 0 警告
- Android 端：`gradlew.bat assembleDebug` BUILD SUCCESSFUL

### 备份
- 创建 `D:\backup\LAN-Audio-Bridge-bak_20260713_0215`（64 个文件，1.00 MB）

### 待继续
- Windows 端剩余：`AudioRouter.cs`、`HandshakeServer.cs`、`MdnsPublisher.cs`、`PacketHeader.cs`、`ProtocolTypes.cs`、`ConnectionStateManager.cs`、`Lpf.cs`
- Android 端剩余：`AudioPipeline.kt`、`AudioEncoder.kt`、`MicrophoneCapturer.kt`、`SystemAudioCapturer.kt`、`LanAudioDiscovery.kt`、`PacketHeader.kt`、`ProtocolTypes.kt`、`UdpSender.kt`、`StreamingService.kt`、`MediaProjectionService.kt`、`Theme.kt`

### 关键技术决策
- Avalonia XAML 中 `Window` 只能有一个 `Content`，TrayIcon 需通过代码动态创建或使用其他方式
- Avalonia 没有 `System.Windows.Media` 命名空间，使用 `Avalonia.Media` 替代
- `Program.LogCrash` 需要 `internal` 访问修饰符才能被其他类访问
